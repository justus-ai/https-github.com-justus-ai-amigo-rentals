services:
  - type: web
    name: amigo-rentals
    env: node
    plan: free
    branch: main
    buildCommand: npm ci && npm run build
    startCommand: npm run preflight:strict && npm start
    autoDeploy: true
